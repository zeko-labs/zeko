(executable
 (name run)
 (libraries
  sequencer_lib
  ;; mina ;;
  init
  ;; opam libraries ;;
  cohttp
  cohttp-async
  graphql
  graphql-async
  core
  core_kernel
  async_kernel
  async
  async_unix
  core_unix.command_unix)
  (preprocess (pps ppx_jane))
  (modules run))
